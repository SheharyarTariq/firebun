import { TilesSkeleton } from "@/components/common/Skeleton";

/** The layout keeps the header and period chips; only the report area is replaced. */
export default function Loading() {
  return <TilesSkeleton />;
}
